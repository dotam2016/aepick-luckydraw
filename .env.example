# Public host port. The existing Survey service already uses 8787.
LUCKYDRAW_PORT=8788
BIND_ADDRESS=0.0.0.0

# Replace both values before starting the container.
ADMIN_KEY=replace-with-a-long-random-secret
OPERATOR_PIN=replace-with-a-private-pin

LOG_LEVEL=info
